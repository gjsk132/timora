#!/bin/bash
# Detach python from the LaunchServices app context so it gets a menu bar slot.
# install.command rewrites DIR to this project's actual location.
DIR="$HOME/timora"
cd "$DIR" || exit 1
nohup "$DIR/venv/bin/python" -m timora >/dev/null 2>&1 &
exit 0
